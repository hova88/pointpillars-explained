export type SourceTag = "PAPER" | "PRIOR WORK" | "IMPLEMENTATION" | "NUSCENES" | "TEACHING MODEL";

export type Chapter = {
  id: string;
  n: number;
  phase: string;
  title: string;
  subtitle: string;
  equation: string;
  shape: string;
  source: SourceTag;
  observe: string;
  operate: string;
  inspect: string;
  why: string;
  compare: string;
  stress: string;
  summary: string;
};

export const chapters: Chapter[] = [
  { id:"points",n:1,phase:"INPUT",title:"LiDAR, points & point clouds",subtitle:"A return is a measurement—not an object.",equation:"pᵢ = (xᵢ, yᵢ, zᵢ, rᵢ)",shape:"N × 4",source:"TEACHING MODEL",observe:"Follow one laser pulse from emission to a reflected return, then expand it into a 360° sweep.",operate:"Convert range and beam direction into Cartesian coordinates; retain reflectance as a fourth measurement.",inspect:"Select any return to read position, range, azimuth, height and intensity in the LiDAR frame.",why:"Geometry is sparse, unordered and sampled only where a beam receives energy back. Those properties are fundamentally unlike a dense image grid.",compare:"Pixels have regular neighbors; mesh vertices have explicit connectivity; voxels impose cells; raw points provide neither adjacency nor order.",stress:"Occlusion, grazing surfaces, dark materials and range create missing or uneven returns. A point is evidence, not a filled surface.",summary:"The detector starts with an unordered N×4 set whose density and coverage vary across space." },
  { id:"nuscenes",n:2,phase:"INPUT",title:"A nuScenes keyframe",subtitle:"Real sensor evidence, three coordinate frames.",equation:"p₍global₎ = T₍ego→global₎ T₍sensor→ego₎ p₍sensor₎",shape:"34,688 raw returns",source:"NUSCENES",observe:"Orbit a reduced copy of the official tutorial LIDAR_TOP keyframe and switch between perspective, BEV and ego views.",operate:"Treat the downloaded binary as rows of x, y, z, intensity and ring index; the teaching asset preserves every sixth return.",inspect:"The sample token, data token, filename and deterministic reduction are exposed in the provenance panel.",why:"A real sweep keeps the lesson grounded in true density falloff, occlusion and street geometry rather than a convenient synthetic cloud.",compare:"Synthetic data is easier to control but can hide the irregular sampling that motivates pillar encoders.",stress:"This browser asset is a visualization subset, not a training sample and not a detector benchmark.",summary:"The same verified frame will be traced through every representation." },
  { id:"range",n:3,phase:"PREPROCESS",title:"Detection range",subtitle:"Spend computation where predictions matter.",equation:"keep ⇔ x∈[xₘᵢₙ,xₘₐₓ) ∧ y∈[yₘᵢₙ,yₘₐₓ) ∧ z∈[zₘᵢₙ,zₘₐₓ)",shape:"N × 4 → N′ × 4",source:"PAPER",observe:"Move the range slider and watch distant returns fade before any feature computation.",operate:"Apply half-open bounds so every accepted point has exactly one valid grid address.",inspect:"The live HUD reports accepted points, clipped points and the resulting grid width and height.",why:"Early filtering reduces memory and prevents useless features outside the evaluation region from entering the network.",compare:"Filtering after encoding wastes compute; infinite range makes tensor size data-dependent; closed upper bounds create an out-of-range final index.",stress:"Too narrow loses objects; too wide spends most cells on sparse distance. Z limits also remove ground artifacts and overhead returns.",summary:"The configured metric range fixes the physical canvas for all later BEV tensors." },
  { id:"grid",n:4,phase:"PREPROCESS",title:"Pillar grid construction",subtitle:"Quantize XY, leave Z continuous.",equation:"iₓ=⌊(x−xₘᵢₙ)/vₓ⌋, iᵧ=⌊(y−yₘᵢₙ)/vᵧ⌋",shape:"H=Δy/vᵧ, W=Δx/vₓ",source:"PAPER",observe:"A metric grid settles over the BEV scene; occupied cells light up as vertical columns.",operate:"Change cell size from 0.16 m to 0.64 m and observe grid resolution, occupancy and point density.",inspect:"Hover a cell to see its integer address and continuous spatial bounds.",why:"Pillars create regular 2D addresses while avoiding hand-designed vertical bins and costly 3D convolution.",compare:"Smaller cells preserve localization but enlarge H×W; 3D voxels preserve height layout but multiply sparse volume; raw neighborhoods need costly searches.",stress:"Quantization makes different within-cell positions share one address. Later center offsets restore part of that lost information.",summary:"Every accepted point now owns a discrete BEV cell but keeps its continuous XYZ values." },
  { id:"assignment",n:5,phase:"PREPROCESS",title:"Point-to-pillar assignment",subtitle:"Sparse gather before dense scatter.",equation:"pillar[(iᵧ,iₓ)] ← pillar[(iᵧ,iₓ)] ∪ {pᵢ}",shape:"N′ points → Pₐ occupied pillars",source:"IMPLEMENTATION",observe:"Points pulse into their addressed columns; empty grid cells remain unallocated.",operate:"Group points with integer coordinate keys, retaining the coordinate list beside the point lists.",inspect:"Select a pillar to isolate its members and local density profile.",why:"Sparse grouping avoids allocating H×W×N storage when most BEV cells contain no return.",compare:"A full dense grid is simpler conceptually but dominated by zeros; nearest-neighbor grouping lacks a reusable image address.",stress:"Hash order is not semantic. Deterministic teaching order must never be confused with point order learned by the model.",summary:"The cloud becomes a sparse list of occupied cells and variable-length point sets." },
  { id:"capacity",n:6,phase:"PREPROCESS",title:"Capacity, sampling & padding",subtitle:"Regular tensors require explicit information loss.",equation:"P=min(Pₐ,Pₘₐₓ), Nⱼ=min(|pillarⱼ|,Nₘₐₓ)",shape:"variable → P × N × D",source:"IMPLEMENTATION",observe:"Dense pillars shed excess points while short pillars receive masked zero rows.",operate:"Adjust N and watch the selected pillar switch between retention, truncation and padding.",inspect:"Real measurements and padding are separated by a validity mask—even when a real coordinate equals zero.",why:"Fixed P and N enable contiguous batches and predictable GPU kernels.",compare:"Ragged tensors preserve all returns but complicate batching; first-N sampling can encode sensor scan order; random sampling adds variance but reduces fixed bias.",stress:"Nearby surfaces are densest and therefore truncated most often. Capacity is a compute budget, not a neutral reshape.",summary:"The encoder accepts a bounded dense tensor plus pillar coordinates and masks." },
  { id:"decoration",n:7,phase:"PILLAR FEATURE NET",title:"Point feature decoration",subtitle:"Keep global position; add two kinds of local context.",equation:"[x,y,z,r, x−x̄,y−ȳ,z−z̄, x−xₚ,y−yₚ]",shape:"D=9 common variant",source:"IMPLEMENTATION",observe:"Choose a point; vectors appear from the pillar centroid and geometric cell center.",operate:"Toggle raw, cluster-offset and pillar-offset groups and inspect the surviving representation.",inspect:"The table computes every feature from the selected point, its data-dependent mean and fixed grid center.",why:"Cluster offsets describe point deviation and are translation-invariant; pillar offsets restore within-cell location; raw XYZ preserves absolute scene position.",compare:"Using x̄ alone gives every point the same local reference and cannot express deviation. Using only x−x̄ removes absolute placement. Using only raw x forces the shared MLP to learn centering itself.",stress:"The original paper describes decorated features, while exact channel order and D=9 are implementation conventions. Toggle effects are representational—not retrained accuracy claims.",summary:"Each return carries sensor evidence, local shape cues and quantization-relative position." },
  { id:"tensor",n:8,phase:"PILLAR FEATURE NET",title:"Stacked pillar tensor",subtitle:"Three axes, three different meanings.",equation:"X[d,p,n]",shape:"D × P × N (notation varies)",source:"IMPLEMENTATION",observe:"Pillar matrices fold into a stack; feature, pillar and point axes receive distinct colors.",operate:"Scrub the tensor slice and trace a cell back to the 3D return that produced it.",inspect:"A miniature 9×6×8 tensor sits beside the deployment-scale dimensions.",why:"Separating axes lets one shared point function run in parallel across every valid point.",compare:"Frameworks may use batch×P×N×D or batch×D×P×N. Layout changes memory access—not algorithmic meaning.",stress:"Padding must be masked before symmetric aggregation or learned biases can turn padded rows into false evidence.",summary:"Geometry is now a batched numerical object with explicit feature, pillar and point axes." },
  { id:"pfn",n:9,phase:"PILLAR FEATURE NET",title:"Shared point transformation",subtitle:"Learn a basis for every return with the same weights.",equation:"hᵢ = ReLU(BN(Wpᵢ+b))",shape:"D × P × N → C × P × N",source:"PAPER",observe:"A selected 9D row fans through learned channels; negative normalized responses are clipped.",operate:"Step through Linear, BatchNorm and ReLU while a value strip updates.",inspect:"Weights are shared across point and pillar indices; only feature channels mix.",why:"A shared MLP is efficient and does not assign meaning to arbitrary point order. BN stabilizes feature scale; ReLU adds a cheap nonlinearity.",compare:"Separate weights per pillar would bind the network to location and explode parameters; fixed encoders cannot adapt; no activation collapses stacked linear layers to one linear map.",stress:"BN uses batch statistics in training and running statistics in evaluation. ReLU discards negative magnitude and can create dead channels.",summary:"Every valid point becomes a learned C-dimensional response under one shared function." },
  { id:"pool",n:10,phase:"PILLAR FEATURE NET",title:"Symmetric aggregation",subtitle:"One descriptor per pillar, independent of point order.",equation:"fₚ[c] = maxₙ h[c,p,n]",shape:"C × P × N → C × P",source:"PAPER",observe:"Each feature channel crowns a possibly different winning point.",operate:"Shuffle point order and compare max, mean and sum pooling live.",inspect:"Winner tracing reveals which return contributed each output channel.",why:"Max is symmetric, density-insensitive and highlights the strongest learned evidence.",compare:"Mean describes average evidence but dilutes rare signals; sum depends on density; attention is richer but adds computation and ordering machinery.",stress:"Pooling destroys within-pillar order and most multiplicity information. No later layer can reconstruct those discarded responses.",summary:"Each occupied cell is represented by one permutation-invariant learned vector." },
  { id:"scatter",n:11,phase:"PSEUDO-IMAGE",title:"Scatter to pseudo-image",subtitle:"Return sparse features to their BEV addresses.",equation:"I[:,iᵧ,iₓ] = fₚ; empty cells = 0",shape:"C × P → C × H × W",source:"PAPER",observe:"Feature cards fly from the sparse list back into their grid cells.",operate:"Switch feature channel and click a pseudo-pixel to trace its source pillar and points.",inspect:"Occupied cells hold learned vectors; zero vectors identify unoccupied spatial locations.",why:"Scatter converts irregular sparse features into the regular image-like tensor expected by optimized 2D CNNs.",compare:"Interpolation invents values between returns; early dense allocation wastes capacity; 3D scatter retains height but demands 3D processing.",stress:"Zero can also be a valid learned vector, so occupancy is implicit unless retained separately. Vertical arrangement is already compressed.",summary:"The LiDAR sweep is now a BEV pseudo-image with learned channels instead of RGB." },
  { id:"backbone",n:12,phase:"BACKBONE",title:"2D CNN backbone",subtitle:"Trade spatial detail for context.",equation:"B(S,L,F): Conv₃×₃(stride) → [Conv₃×₃]ᴸ⁻¹",shape:"H/2→H/4→H/8; C→2C→4C",source:"PAPER",observe:"Feature maps shrink while receptive-field windows expand over more physical roadway.",operate:"Adjust a stage stride and observe output dimensions and relative operation count.",inspect:"Every block reports stride, channels, cells and effective receptive field.",why:"Downsampling makes broad context affordable: object identity needs a larger region than one pillar.",compare:"Full-resolution convolution preserves localization but costs much more; dense 3D CNNs process an additional mostly empty dimension; sparse 3D CNNs are flexible but less hardware-simple.",stress:"Aggressive stride can erase small pedestrians and blur close objects. Context is purchased with spatial precision.",summary:"The top-down path builds increasingly semantic, increasingly coarse BEV features." },
  { id:"fusion",n:13,phase:"BACKBONE",title:"Upsampling & fusion",subtitle:"Align scales before combining them.",equation:"F = Concat(Up(F₁), Up(F₂), Up(F₃))",shape:"three H/2 maps → H/2 × W/2 × 6C",source:"PAPER",observe:"Three stage outputs rise to a common resolution and lock into one channel stack.",operate:"Toggle a scale to expose the spatial detail or context it contributes.",inspect:"The shape ledger shows why concatenation changes channels but not H or W.",why:"Shallow maps localize; deep maps recognize. Multi-scale fusion supplies both to the dense head.",compare:"Interpolation+conv can avoid transposed-convolution artifacts; summation is cheaper but requires aligned channels and loses scale identity; no fusion starves the head of either detail or context.",stress:"Kernel/stride mismatch can create checkerboard patterns. That is a general upsampling issue, not a PointPillars-only defect.",summary:"A single multi-scale BEV feature map feeds every prediction branch." },
  { id:"head",n:14,phase:"DETECTION",title:"SSD detection head",subtitle:"Dense one-stage predictions at every BEV location.",equation:"channels = A·K + A·7 + A·2",shape:"feature map → class, box, direction maps",source:"PAPER",observe:"Each feature-map cell unfolds anchors and three parallel output branches.",operate:"Change anchor count A or class count K and see output channels recompute.",inspect:"Select one output scalar and trace it to class, anchor, parameter and physical center.",why:"A one-stage head avoids a separate proposal pipeline and keeps inference fast.",compare:"Two-stage detectors refine sparse proposals but add latency; anchor-free heads remove template design but were not the paper’s choice.",stress:"Dense heads create extreme foreground/background imbalance and many overlapping candidates—later loss design and NMS must handle both.",summary:"The head predicts what, where and which heading bin for every anchor." },
  { id:"anchors",n:15,phase:"DETECTION",title:"Anchor design",subtitle:"Templates turn unconstrained boxes into residuals.",equation:"a=(xₐ,yₐ,zₐ,wₐ,lₐ,hₐ,θₐ)",shape:"A anchors per BEV cell",source:"PRIOR WORK",observe:"Class-shaped boxes tile the BEV grid at multiple orientations.",operate:"Alter anchor dimensions and yaw; match quality and candidate count respond.",inspect:"Anchor centers come from feature locations while dimensions and orientations encode priors.",why:"Priors make regression local and give every output a clear geometric hypothesis.",compare:"Too few templates hurt initial overlap and recall; too many multiply outputs and false positives; anchor-free centers avoid templates but need different targets.",stress:"KITTI class statistics are not nuScenes statistics. Dataset-specific templates are adaptations, not universal PointPillars constants.",summary:"Anchors discretize box hypotheses so the network predicts bounded corrections." },
  { id:"matching",n:16,phase:"TRAINING",title:"Ground-truth matching",subtitle:"Assignment decides which examples the loss can teach from.",equation:"label(a)=positive if IoU≥t₊; negative if IoU<t₋",shape:"anchors → {+, ignore, −}",source:"PRIOR WORK",observe:"Anchors turn green, amber or grey as overlap thresholds sweep across them.",operate:"Drag IoU thresholds and calculate intersection, union and assignment for a selected pair.",inspect:"The loss ledger updates counts of positive, ignored and negative examples.",why:"Explicit assignment converts boxes into supervised targets while avoiding contradictory gradients in the ambiguous band.",compare:"A single threshold has no ignore buffer; nearest-anchor rules guarantee positives; anchor-free heatmaps encode objects differently.",stress:"Thresholds are class-sensitive. Tiny objects can have low IoU after small center errors, creating fewer positives.",summary:"Matching defines the training population—it is not merely a bookkeeping step." },
  { id:"box-code",n:17,phase:"TRAINING",title:"Box parameterization",subtitle:"Predict normalized residuals, not world coordinates.",equation:"Δx=(x−xₐ)/√(wₐ²+lₐ²), Δw=log(w/wₐ)",shape:"7 encoded residuals per anchor",source:"PRIOR WORK",observe:"A target box decomposes into center arrows, log-scale changes and yaw rotation.",operate:"Move or resize the target and watch all seven encoded values update.",inspect:"Decode reconstructs the box by reversing normalization and logarithms.",why:"Residuals reduce target scale variation; diagonal normalization treats XY offsets relative to anchor footprint; logarithms make size ratios symmetric.",compare:"Direct coordinates mix meter scales with size scales and location range; unnormalized residuals make large anchors tolerate the same error differently.",stress:"Yaw is periodic and discontinuous near wrap boundaries. Direction classification handles a remaining 180° ambiguity.",summary:"Box regression learns a dimensionless correction around a geometric prior." },
  { id:"direction",n:18,phase:"TRAINING",title:"Direction classification",subtitle:"Resolve heading flips that regression alone handles poorly.",equation:"dir = bin(wrap(θ)); θ̂ = residual + bin offset",shape:"2 logits per anchor",source:"PAPER",observe:"A car box flips 180° despite similar geometry, then the direction bin corrects it.",operate:"Rotate across the π boundary and compare residual-only, binned and sin/cos encodings.",inspect:"The panel separates continuous yaw residual from discrete heading bin.",why:"Oriented boxes can have nearly identical footprints while facing opposite directions; a small auxiliary classification makes the discontinuity explicit.",compare:"Sin/cos is continuous at wrap-around but needs normalization and still represents symmetric objects ambiguously.",stress:"Direction labels do not create front/back evidence when the point cloud itself is ambiguous.",summary:"Continuous regression estimates angle locally; classification selects the correct half-turn." },
  { id:"loss",n:19,phase:"TRAINING",title:"Loss functions",subtitle:"Balance rare objects, robust geometry and heading.",equation:"L = βclsLcls + βlocLloc + βdirLdir",shape:"many anchor losses → scalar",source:"PAPER",observe:"Easy negatives fade under focal weighting while a hard positive remains bright.",operate:"Adjust focal α, γ and Smooth L1 β; live curves and per-example contributions update.",inspect:"Select an anchor to see its label, probability, residual error and each weighted term.",why:"Focal loss addresses dense class imbalance; Smooth L1 is quadratic near zero and linear for outliers; direction CE handles discrete bins.",compare:"Plain CE lets abundant easy negatives dominate; L2 overweights large errors; L1 has a constant gradient around zero.",stress:"Loss weights change optimization, not output units. Reported settings belong to a dataset/training recipe, not the pillar representation itself.",summary:"The training objective coordinates recognition, localization and orientation under severe imbalance." },
  { id:"augmentation",n:20,phase:"TRAINING",title:"Augmentation & GT sampling",subtitle:"Create plausible variation without breaking geometry.",equation:"p′=sRθp+t; box′=sRθbox+t",shape:"scene → augmented scene",source:"PRIOR WORK",observe:"Objects are sampled into a scene, checked for collision, then transformed with every point and box together.",operate:"Apply rotation, scaling, translation and mirror operations independently.",inspect:"A dependency checklist highlights every quantity that must transform consistently.",why:"Rare objects need more positive examples; global transforms teach invariance within physically plausible ranges.",compare:"Image-only transforms cannot be copied blindly to 3D; moving labels without points creates impossible supervision; unchecked database insertion creates collisions.",stress:"Aggressive scaling changes object realism; ground-plane mismatch makes pasted objects float or sink.",summary:"Augmentation expands training support while preserving point–box geometry." },
  { id:"training",n:21,phase:"TRAINING",title:"End-to-end training flow",subtitle:"Fixed geometry outside, learned functions inside.",equation:"θ ← θ − η∇θL",shape:"batch → predictions → loss → gradients",source:"PAPER",observe:"A pulse travels forward through preprocessing and the network, then gradients return only through differentiable learned blocks.",operate:"Toggle train/eval mode to expose BN statistics, augmentation and post-processing differences.",inspect:"The graph labels learned weights, fixed hyperparameters and training-only nodes.",why:"End-to-end learning aligns pillar features with the downstream detection objective rather than optimizing a handcrafted encoder separately.",compare:"Fixed encoders are predictable and fast but cannot adapt their feature basis; modular training can simplify debugging but may optimize mismatched objectives.",stress:"Sampling and hard assignment are not differentiated. Their choices still shape gradients by changing the data that reaches the loss.",summary:"The PFN, backbone and head learn jointly; grid construction and target generation define their interface." },
  { id:"decode",n:22,phase:"INFERENCE",title:"Scores, filtering & decoding",subtitle:"Turn dense outputs into physical candidates.",equation:"score=σ(logit); keep if score≥τ",shape:"dense maps → candidate boxes",source:"IMPLEMENTATION",observe:"Logits become probabilities; low-confidence anchors vanish before box decoding is displayed.",operate:"Move the class threshold and compare candidate count, precision pressure and recall risk.",inspect:"A selected candidate shows logit, sigmoid score, anchor, residual and decoded metric box.",why:"Early thresholding makes NMS cheaper and removes candidates unlikely to survive.",compare:"A single global threshold is simple but classes may be differently calibrated; filtering after NMS spends overlap computation on obvious negatives.",stress:"Scores are not guaranteed calibrated probabilities. Raising τ can hide true distant or small objects.",summary:"Inference converts learned residuals into scored 3D proposals in the sensor frame." },
  { id:"nms",n:23,phase:"INFERENCE",title:"Non-maximum suppression",subtitle:"Many hypotheses, one retained explanation.",equation:"suppress bⱼ if IoU(bbest,bⱼ)>τNMS",shape:"M candidates → K detections",source:"PAPER",observe:"Candidates sort by score; the best box survives while overlapping neighbors are crossed out round by round.",operate:"Adjust IoU threshold and switch axis-aligned approximation, rotated boxes or soft decay.",inspect:"The overlap panel separates intersection and union for the active pair.",why:"Dense anchors intentionally produce duplicates; NMS removes them with a simple greedy rule. The paper uses fast axis-aligned BEV overlap for inference.",compare:"Rotated IoU is geometrically faithful but costlier; soft NMS decays instead of deleting; class-wise NMS prevents unrelated classes suppressing each other.",stress:"Two nearby real objects can suppress one another, while poorly overlapping duplicates can both survive.",summary:"NMS is a heuristic selection layer, not learned reasoning about object identity." },
  { id:"evaluation",n:24,phase:"OUTPUT",title:"Predictions & evaluation",subtitle:"A box can be wrong in several independent ways.",equation:"prediction = (class, score, x,y,z,w,l,h,θ)",shape:"K final detections",source:"NUSCENES",observe:"Retained boxes return to the point cloud; error overlays separate class, center, scale and orientation mistakes.",operate:"Change confidence and NMS thresholds to see false-positive/false-negative pressure.",inspect:"A diagnostic card names the dominant error mode for selected examples.",why:"Aggregate precision alone hides whether a model misses objects or merely localizes them poorly.",compare:"KITTI AP uses IoU-based protocols; nuScenes reports center-distance matching and multiple true-positive error terms. Their numbers must not be mixed.",stress:"This lesson frame is not a benchmark. Visual plausibility cannot replace dataset evaluation.",summary:"Evaluation interprets final boxes under a dataset-specific protocol—not a universal score." },
  { id:"tradeoffs",n:25,phase:"SYNTHESIS",title:"Tradeoffs & limitations",subtitle:"Speed comes from deliberate compression.",equation:"3D points → sparse pillars → dense 2D features",shape:"irregular 3D → regular BEV",source:"PAPER",observe:"A final ledger marks every compression: quantization, capacity limits, pooling, stride and NMS.",operate:"Compare PointPillars with fixed BEV, VoxelNet, raw-point and later pillar-based designs across representation and compute.",inspect:"Select a limitation to trace back to the exact operation that introduced it.",why:"The design wins speed by making the expensive learned pipeline almost entirely 2D and GPU-friendly.",compare:"Voxel methods retain explicit height bins; raw-point methods avoid grid quantization; later pillar detectors change backbones, heads and assignment while preserving the core BEV idea.",stress:"Small, distant and vertically complex objects expose compression limits. Fast does not mean information-preserving.",summary:"PointPillars is a sequence of reasoned tradeoffs—not a single trick and not a universally optimal representation." },
];

export const phases = ["INPUT","PREPROCESS","PILLAR FEATURE NET","PSEUDO-IMAGE","BACKBONE","DETECTION","TRAINING","INFERENCE","OUTPUT","SYNTHESIS"];
